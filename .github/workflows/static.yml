name: "NRC Protein-Folding: 100% Integrity Suite & Deployment"

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # ------------------------------------------------------------
  # Job 1: Mathematical & Biological Integrity
  # ------------------------------------------------------------
  verify:
    name: "Bio-Lattice Integrity Audit"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Dependencies
        run: |
          pip install "nrc @ git+https://github.com/Nexus-Resonance-Codex/NRC.git"
          pip install -e ".[dev]"
      - name: Run Tests
        run: |
          pytest tests/ -v

  # ------------------------------------------------------------
  # Job 2: Documentation & Showcase Deployment
  # ------------------------------------------------------------
  deploy-site:
    name: "Deploy Flagship Documentation"
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Documentation Engines
        run: |
          pip install mkdocs-material mkdocs-minify-plugin
      - name: Build MkDocs Site
        run: |
          mkdocs build
      - name: Integrate Static Showcase
        run: |
          # Ensure showcase is accessible at root or expected paths
          cp docs/demos/protein-folding-showcase.html site/showcase.html
          cp docs/demos/lattice-visualizer.html site/lattice.html
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "site"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
